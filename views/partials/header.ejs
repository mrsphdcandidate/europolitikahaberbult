<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <link rel="icon" type="image/png" href="/img/favicon.png">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof pageTitle !== 'undefined' && pageTitle ? pageTitle + ' — EuroPolitika' : 'EuroPolitika — Politik Ekonomi Bülteni' %></title>
  <meta name="description" content="<%= typeof pageDescription !== 'undefined' && pageDescription ? pageDescription : 'Politik ekonomi dünyasından en güncel analizler ve haberler.' %>">
  <meta property="og:title" content="<%= typeof pageTitle !== 'undefined' && pageTitle ? pageTitle + ' — EuroPolitika' : 'EuroPolitika — Politik Ekonomi Bülteni' %>">
  <meta property="og:description" content="<%= typeof pageDescription !== 'undefined' && pageDescription ? pageDescription : 'Politik ekonomi dünyasından en güncel analizler ve haberler.' %>">
  <meta property="og:type" content="<%= typeof ogType !== 'undefined' ? ogType : 'website' %>">
  <% if (typeof ogImage !== 'undefined' && ogImage) { %>
  <meta property="og:image" content="<%= ogImage %>">
  <% } %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Playfair+Display:wght@400;600;700;800;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css">
  <script>
    (function() {
      const theme = localStorage.getItem('theme') || 'light';
      document.documentElement.setAttribute('data-theme', theme);
    })();
  </script>
</head>
<body>
  <!-- Header -->
  <header class="site-header">
    <div class="header-inner">
      <a href="/" class="site-logo">
        <img src="/img/logo_light.svg" class="logo-light site-logo-img" alt="EuroPolitika Logo">
        <img src="/img/logo_dark.svg" class="logo-dark site-logo-img" alt="EuroPolitika Logo">
      </a>
      <div style="display: flex; align-items: center; gap: 16px;">
        <button id="themeToggle" type="button" style="background: none; border: none; font-size: 20px; cursor: pointer; padding: 0; outline: none; line-height: 1;">🌙</button>
        <a href="#subscribe" class="btn-subscribe-header">Abone Ol</a>
      </div>
    </div>
  </header>

  <main>
